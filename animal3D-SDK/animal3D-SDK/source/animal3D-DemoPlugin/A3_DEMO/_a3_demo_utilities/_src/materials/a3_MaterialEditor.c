#include "..\..\materials\a3_MaterialEditor.h"


void a3materialParseFile(a3_RenderMaterial* mat, const a3byte* data)
{
	/*
		a3filestreamReadObject(fileStream, tempMaterial, (a3_FileStreamReadFunc)a3materialParseFile); //calls the read func to parse the actual file

		parsing function will have:
		

	//sizeof(char) == sizeof(byte);
	//Uniforms = 3
	//	Uniform
	//	Uniform
	//	Uniform
	//	Material
	//	Material
	*/

	
}
